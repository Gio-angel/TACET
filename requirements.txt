
# --- UI (local web UI in a native window) ---
pywebview

# --- ASR: lighter / faster than openai-whisper ---
faster-whisper

# --- microphone capture ---
sounddevice
numpy

# --- frozen BERT encoder ---
torch
transformers

# --- packaging  ---
# pyinstaller

#pitching
librosa

# --- LLM API: Gemini (set key in .env) ---
google-generativeai
python-dotenv

# --- TTS: natural neural voice (free, no key, needs internet) ---
edge-tts
pygame-ce            # prebuilt wheels for new Python; drop-in for pygame

# --- training: metric/plot generation (training/) ---
matplotlib

# --- dataset tooling (only for utils/ scripts: encode + generate datasets) ---
pandas
pyarrow              # read the .parquet seed
num2words            # spell out numbers when cleaning text
